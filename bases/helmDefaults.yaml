---
helmDefaults:
  atomic: true
  tillerNamespace: kube-system
  tillerless: true
  cleanupOnFail: false
  wait: true
  timeout: 3600
  # Do not recreate pods during upgrade when not required
  recreatePods: false
