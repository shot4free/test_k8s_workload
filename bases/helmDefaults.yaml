---
helmDefaults:
  atomic: false
  tillerNamespace: kube-system
  tillerless: true
  cleanupOnFail: false
  wait: false
  timeout: 3600
  # Do not recreate pods during upgrade when not required
  recreatePods: false
