---
{{ readFile "gprd.yaml.gotmpl" | fromYaml | toYaml }}

gitlab:
  mailroom:
    enabled: false

  sidekiq:
    enabled: false
registry:
  hpa:
    minReplicas: 5
    maxReplicas: 150
  nodeSelector:
    type: registry
  resources:
    requests:
      cpu: 250m
      memory: 3G
