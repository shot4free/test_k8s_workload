---
{{ readFile "gprd.yaml.gotmpl" | fromYaml | toYaml }}

gitlab:
  mailroom:
    enabled: false

  sidekiq:
    enabled: false

registry:
  hpa:
    minReplicas: 2
    maxReplicas: 150
  resources:
    requests:
      cpu: 250m
      memory: 600M
