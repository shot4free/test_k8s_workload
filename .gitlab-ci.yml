---

# Generated by "make generate-ci-config"
# Do not edit!!

.gprd:
  environment:
    name: gprd
  extends:
    - .gprd-base
  resource_group: gprd
  variables:
    CLUSTER: gprd-gitlab-gke
    REGION: us-east1
.gprd-base:
  environment:
    url: https://gitlab.com
  variables:
    ENVIRONMENT_WITHOUT_STAGE: gprd
    PROJECT: gitlab-production
.gprd-us-east1-b:
  environment:
    name: gprd-us-east1-b
  extends:
    - .gprd-base
  resource_group: gprd-us-east1-b
  variables:
    CLUSTER: gprd-us-east1-b
    REGION: us-east1-b
.gprd-us-east1-c:
  environment:
    name: gprd-us-east1-c
  extends:
    - .gprd-base
  resource_group: gprd-us-east1-c
  variables:
    CLUSTER: gprd-us-east1-c
    REGION: us-east1-c
.gprd-us-east1-d:
  environment:
    name: gprd-us-east1-d
  extends:
    - .gprd-base
  resource_group: gprd-us-east1-d
  variables:
    CLUSTER: gprd-us-east1-d
    REGION: us-east1-d
.gstg:
  environment:
    name: gstg
  extends:
    - .gstg-base
  resource_group: gstg
  variables:
    CLUSTER: gstg-gitlab-gke
    REGION: us-east1
.gstg-base:
  environment:
    url: https://staging.gitlab.com
  variables:
    ENVIRONMENT_WITHOUT_STAGE: gstg
    PROJECT: gitlab-staging-1
.gstg-us-east1-b:
  environment:
    name: gstg-us-east1-b
  extends:
    - .gstg-base
  resource_group: gstg-us-east1-b
  variables:
    CLUSTER: gstg-us-east1-b
    REGION: us-east1-b
.gstg-us-east1-c:
  environment:
    name: gstg-us-east1-c
  extends:
    - .gstg-base
  resource_group: gstg-us-east1-c
  variables:
    CLUSTER: gstg-us-east1-c
    REGION: us-east1-c
.gstg-us-east1-d:
  environment:
    name: gstg-us-east1-d
  extends:
    - .gstg-base
  resource_group: gstg-us-east1-d
  variables:
    CLUSTER: gstg-us-east1-d
    REGION: us-east1-d
.pre:
  environment:
    name: pre
  extends:
    - .pre-base
  resource_group: pre
  variables:
    CLUSTER: pre-gitlab-gke
.pre-base:
  environment:
    name: pre
    url: https://pre.gitlab.com
  variables:
    ENVIRONMENT_WITHOUT_STAGE: pre
    PROJECT: gitlab-pre
    REGION: us-east1
assert_consistent_tool_versions:
  allow_failure: true
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_COMMIT_REF_NAME == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_API_V4_URL == "https://ops.gitlab.net/api/v4"
      when: never
    - when: on_success
  script: /k8s-workloads/assert-tool-versions.sh
  stage: check
assert_formatting:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://ops.gitlab.net/api/v4"
      when: never
    - when: on_success
  script: |
    find . -name '*.*sonnet' | xargs -n1 jsonnetfmt -i
    git diff --exit-code
  stage: check
bundler-audit-dependency_scanning:
  stage: check
check-vendored-charts:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_COMMIT_REF_NAME == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_API_V4_URL == "https://ops.gitlab.net/api/v4"
      when: never
    - when: on_success
  script: |
    chmod 400 "$SSH_PRIVATE_KEY"
    export GIT_SSH_COMMAND="ssh -i $SSH_PRIVATE_KEY -o IdentitiesOnly=yes -o GlobalKnownHostsFile=$SSH_KNOWN_HOSTS"
    vendir sync
    for i in pre gstg gprd;do pushd vendor/charts/gitlab/$i;helm dep update;popd;done
    git diff --exit-code vendor || (echo "Charts in this commit are not the same as upstream" >&2 && exit 1)
  stage: check
ci_config_generated:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  script: |
    make generate-ci-config
    git diff --exit-code || (echo "Please run 'make generate-ci-config'" >&2 && exit 1)
  stage: check
dependency_scanning:
  stage: check
gprd-cny:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gprd-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd-cny" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -s cny upgrade
  stage: gprd-cny:deploy
  tags:
    - k8s-workloads
gprd-cny:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gprd-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -s cny template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gprd-cny:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gprd-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D -s cny upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-cny:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gprd-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd-cny" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D -s cny upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-cny:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gprd-cny
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl -s cny upgrade
  stage: gprd-cny:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gprd-us-east1-b:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: gprd:deploy:alpha
  tags:
    - k8s-workloads
gprd-us-east1-b:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-b:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-b:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-b:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: gprd:deploy:alpha
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gprd-us-east1-c:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: gprd:deploy:beta
  tags:
    - k8s-workloads
gprd-us-east1-c:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-c:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-c:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-c:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: gprd:deploy:beta
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gprd-us-east1-d:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: gprd:deploy:beta
  tags:
    - k8s-workloads
gprd-us-east1-d:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-d:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-d:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd-us-east1-d:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: gprd:deploy:beta
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gprd:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: gprd:deploy:alpha
  tags:
    - k8s-workloads
gprd:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gprd:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gprd" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gprd:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gprd
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: gprd:deploy:alpha
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gstg-cny:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gstg-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg-cny" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -s cny upgrade
  stage: non-prod-cny:deploy
  tags:
    - k8s-workloads
gstg-cny:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gstg-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -s cny template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gstg-cny:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gstg-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D -s cny upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-cny:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gstg-cny
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg-cny" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D -s cny upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-cny:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  resource_group: gstg-cny
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl -s cny upgrade
  stage: non-prod-cny:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gstg-us-east1-b:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
gstg-us-east1-b:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-b:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-b:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-b:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-b
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gstg-us-east1-c:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
gstg-us-east1-c:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-c:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-c:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-c:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-c
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gstg-us-east1-d:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
gstg-us-east1-d:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-d:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-d:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg-us-east1-d:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg-us-east1-d
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
gstg:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
gstg:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
gstg:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "gstg" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
gstg:qa:
  image: alpine:3.16
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $EXPEDITE_DEPLOYMENT
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/gstg/**/*
        - vendor/charts/gitlab-runner/gstg/**/*
        - .gitlab-ci.yml
        - '*.yaml'
        - '*.yml'
        - bases/**/*
        - bin/**/*
        - releases/gitlab/helmfile.yaml
        - releases/gitlab/values/values*
        - releases/gitlab/values/gstg*
      when: on_success
  script: |
    if [[ $SKIP_QA == "true" ]]; then
      echo "Skipping QA because SKIP_QA is set to true"
      exit 0
    fi
    apk add curl jq
    # project=full/path/to/project
    echo "Sending trigger to $project"
    # URL encode the project
    project=$(echo -n "$project" | jq -sRr @uri)
    trigger_url="$CI_API_V4_URL/projects/$project/trigger/pipeline"
    resp=$(curl -s --request POST --form "variables[SMOKE_ONLY]=true" --form "token=$CI_JOB_TOKEN" --form ref=master $trigger_url)
    id=$(echo "$resp" | jq -r ".id")
    web_url=$(echo "$resp" | jq -r ".web_url")
    echo "Waiting for pipeline $web_url ..."
    status_url="$CI_API_V4_URL/projects/$project/pipelines/$id"
    for retry in $(seq 1 120); do
      resp=$(curl -s --header "PRIVATE-TOKEN: $GITLAB_OPS_API_TOKEN" "$status_url")
      status=$(echo "$resp" | jq -r '.status')
      echo "Got pipeline status $status, retry $retry/10"
      [[ $status == "success" || $status == "failed" || $status == "canceled" ]] && break
      sleep 30
    done
    if [[ $status != "success" ]]; then
      echo "$web_url has status $status, failing"
      exit 1
    fi
  stage: non-prod:QA
  variables:
    project: gitlab-org/quality/staging
gstg:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .gstg
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
include:
  - template: Security/Dependency-Scanning.gitlab-ci.yml
  - template: Workflows/Branch-Pipelines.gitlab-ci.yml
notify_com_mr:
  allow_failure: true
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/notify-mr:${CI_IMAGE_VERSION}
  rules:
    - if: $EXPEDITE_DEPLOYMENT
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - when: on_success
  script: |
    notify-mr -s
  stage: check
open-chart-bump-mr-gprd:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - when: never
  script: |
    chmod 400 "$SSH_PRIVATE_KEY"
    export GIT_SSH_COMMAND="ssh -i $SSH_PRIVATE_KEY -o IdentitiesOnly=yes -o GlobalKnownHostsFile=$SSH_KNOWN_HOSTS"
    git config --global user.email "ops@ops.gitlab.net"
    git config --global user.name "ops-gitlab-net"
    git remote set-url origin https://ops-gitlab-net:${GITLAB_API_TOKEN}@gitlab.com/gitlab-com/gl-infra/k8s-workloads/gitlab-com.git
    echo $GITLAB_API_TOKEN | glab auth login --hostname gitlab.com --stdin
    glab config set git_protocol https
    glab auth status
    ./bin/autobump-gitlab-chart.sh gprd
  stage: scheduled
open-chart-bump-mr-gstg:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - when: never
  script: |
    chmod 400 "$SSH_PRIVATE_KEY"
    export GIT_SSH_COMMAND="ssh -i $SSH_PRIVATE_KEY -o IdentitiesOnly=yes -o GlobalKnownHostsFile=$SSH_KNOWN_HOSTS"
    git config --global user.email "ops@ops.gitlab.net"
    git config --global user.name "ops-gitlab-net"
    git remote set-url origin https://ops-gitlab-net:${GITLAB_API_TOKEN}@gitlab.com/gitlab-com/gl-infra/k8s-workloads/gitlab-com.git
    echo $GITLAB_API_TOKEN | glab auth login --hostname gitlab.com --stdin
    glab config set git_protocol https
    glab auth status
    ./bin/autobump-gitlab-chart.sh gstg
  stage: scheduled
open-chart-bump-mr-pre:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - when: never
  script: |
    chmod 400 "$SSH_PRIVATE_KEY"
    export GIT_SSH_COMMAND="ssh -i $SSH_PRIVATE_KEY -o IdentitiesOnly=yes -o GlobalKnownHostsFile=$SSH_KNOWN_HOSTS"
    git config --global user.email "ops@ops.gitlab.net"
    git config --global user.name "ops-gitlab-net"
    git remote set-url origin https://ops-gitlab-net:${GITLAB_API_TOKEN}@gitlab.com/gitlab-com/gl-infra/k8s-workloads/gitlab-com.git
    echo $GITLAB_API_TOKEN | glab auth login --hostname gitlab.com --stdin
    glab config set git_protocol https
    glab auth status
    ./bin/autobump-gitlab-chart.sh pre
  stage: scheduled
pre:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .pre
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "pre" && $DRY_RUN == "false" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
pre:check-label-taxonomy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .pre
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl  template
    ./bin/check-label-taxonomy.sh
  stage: dryrun
  tags:
    - k8s-workloads
pre:dryrun:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .pre
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
pre:dryrun:auto-deploy:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .pre
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $ENVIRONMENT == "pre" && $AUTO_DEPLOY == "true" && $CI_PIPELINE_SOURCE != "schedule"
      when: on_success
  script: |
    bin/k-ctl -D  upgrade
  stage: dryrun
  tags:
    - k8s-workloads
pre:qa:
  allow_failure: true
  image: alpine:3.16
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $EXPEDITE_DEPLOYMENT
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/pre/**/*
        - vendor/charts/gitlab-runner/pre/**/*
        - .gitlab-ci.yml
        - '*.yaml'
        - '*.yml'
        - bases/**/*
        - bin/**/*
        - releases/gitlab/helmfile.yaml
        - releases/gitlab/values/values*
        - releases/gitlab/values/pre*
      when: on_success
  script: |
    if [[ $SKIP_QA == "true" ]]; then
      echo "Skipping QA because SKIP_QA is set to true"
      exit 0
    fi
    apk add curl jq
    # project=full/path/to/project
    echo "Sending trigger to $project"
    # URL encode the project
    project=$(echo -n "$project" | jq -sRr @uri)
    trigger_url="$CI_API_V4_URL/projects/$project/trigger/pipeline"
    resp=$(curl -s --request POST --form "variables[SMOKE_ONLY]=true" --form "token=$CI_JOB_TOKEN" --form ref=master $trigger_url)
    id=$(echo "$resp" | jq -r ".id")
    web_url=$(echo "$resp" | jq -r ".web_url")
    echo "Waiting for pipeline $web_url ..."
    status_url="$CI_API_V4_URL/projects/$project/pipelines/$id"
    for retry in $(seq 1 120); do
      resp=$(curl -s --header "PRIVATE-TOKEN: $GITLAB_OPS_API_TOKEN" "$status_url")
      status=$(echo "$resp" | jq -r '.status')
      echo "Got pipeline status $status, retry $retry/10"
      [[ $status == "success" || $status == "failed" || $status == "canceled" ]] && break
      sleep 30
    done
    if [[ $status != "success" ]]; then
      echo "$web_url has status $status, failing"
      exit 1
    fi
  stage: non-prod:QA
  variables:
    project: gitlab-org/quality/preprod
pre:upgrade:
  before_script: |
    if [[ $LOG_LEVEL == "debug" ]]; then
      echo "A high debug level may expose secrets, this job will now exit..."
      exit 1
    fi
    eval $(./bin/k-ctl config 2>/dev/null)
    if [[ $DRY_RUN == "false" ]]; then
      echo 'Using SERVICE_KEY'
      gcloud auth activate-service-account --key-file $SERVICE_KEY
    else
      echo 'Using SERVICE_KEY_RO'
      gcloud auth activate-service-account --key-file $SERVICE_KEY_RO
    fi
    gcloud config set project $PROJECT
    gcloud container clusters get-credentials ${CLUSTER} --region ${REGION}
  extends:
    - .pre
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_DEFAULT_BRANCH != $CI_COMMIT_REF_NAME
      when: never
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - changes:
        - vendor/charts/gitlab/**/*
        - bases/**/*
        - bin/**/*
        - releases/**/*
        - '*.yaml'
        - '*.yml'
        - .gitlab-ci.yml
      when: on_success
  script: |
    bin/grafana-annotate -e $CI_ENVIRONMENT_NAME
    bin/k-ctl  upgrade
  stage: non-prod:deploy
  tags:
    - k8s-workloads
  variables:
    DRY_RUN: "false"
remove-expedite-variable:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $CI_API_V4_URL == "https://gitlab.com/api/v4"
      when: never
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: ($CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH) && $EXPEDITE_DEPLOYMENT
  script: |
    curl --fail --header "PRIVATE-TOKEN: ${OPS_API_TOKEN}" -X DELETE "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/variables/EXPEDITE_DEPLOYMENT"
  stage: cleanup
retire-js-dependency_scanning:
  stage: check
shellcheck:
  image:
    entrypoint:
      - /bin/sh
      - -c
    name: koalaman/shellcheck-alpine:v0.8.0
  rules:
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_COMMIT_REF_NAME == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - when: on_success
  script:
    - find bin/ -maxdepth 1 -type f | xargs -r shellcheck -e SC1090,SC1091
  stage: check
shfmt:
  image:
    entrypoint:
      - /bin/sh
      - -c
    name: mvdan/shfmt:v3.5.1-alpine
  rules:
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_COMMIT_REF_NAME == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - when: on_success
  script:
    - find bin/ -maxdepth 1 -type f | xargs -r shfmt -i 2 -ci -l -d
  stage: check
stages:
  - check
  - dryrun-check
  - dryrun
  - non-prod-cny:deploy
  - non-prod:deploy
  - non-prod:QA
  - gprd-cny:deploy
  - gprd:deploy:alpha
  - gprd:deploy:beta
  - cleanup
  - scheduled
tooling:
  image: ${CI_REGISTRY}/gitlab-com/gl-infra/k8s-workloads/common/k8-helm-ci:${CI_IMAGE_VERSION}
  rules:
    - if: $AUTO_DEPLOY == "true"
      when: never
    - if: $CI_COMMIT_REF_NAME == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - when: on_success
  script:
    - bin/k-ctl -t
  stage: check
variables:
  AUTO_DEPLOY: "false"
  CI_IMAGE_VERSION: v17.3.0
