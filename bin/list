#!/usr/bin/env bash

##############################
# Lists helm charts

set -euf -o pipefail
dir="$(cd "$(dirname "${0}")"; pwd)"

source "$dir/.setup.bash"
source "$dir/.gitlab.bash"

set -x
helm tiller run helm list "$@"
